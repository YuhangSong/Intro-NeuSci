# 项目说明

**接手/换工作空间：先读根目录 [`HANDOFF.md`](HANDOFF.md)**——当前进度、发布仪式、新增一讲的完整清单、两讲已讲内容、未决事项都在那里。

南京大学人工智能学院本科选修课《神经科学导论》的全部课程材料。每讲一个目录（`lectures/lecNN-*/index.html`），自包含交互网页讲义。

## 讲义的行文铁律（每一讲都必须遵守）

讲义是**面向课堂投影和学生自学的 slide**，不是写给老师的教案。任何"教案味/AI 味"的东西都不能出现在页面上：

- **不标时间预算**。不写"约 X 分钟""开场约 10 分钟"这类每节耗时标注（"第一课时/第二课时"的结构标签可以保留）。
- **不写老师舞台指示**。"课堂：先举手表决，再揭晓"这类话是给老师看的，学生页面上不能有。
- **不用规划口吻描述教学**。反例："课堂上只讲两个故事就够了……其余节点留给你课后点开"；正例："这两个故事课上讲，其余节点课后自己点开看"。页面直接对学生说话，不描述"这节课打算怎么教"。
- **不用叙事工程术语**。"伏笔""回收""悬念：""通关判据""论点零件"这类词是设计文档语言，改用自然的课堂语言（"先记下这句话""你应该看到""已测得"）。
- **不堆"诚实"式自我标榜**。"诚实脚注""诚实的边界"写成普通的"脚注""边界"或直接陈述。
- 交叉引用用"第 N 节""最后一节""开头/下课前"这类课堂语言，不用"100 分钟后"这类排片语言。
- 术语首次出现给英文（`<span class="term">`），单位全文一致。

内容哲学：**少而精**。一讲（2×50 分钟）只讲透一件事；每个交互都要有"先预测 → 动手 → 对照"的闭环和目标形任务；机制展示优先于知识罗列。

## 发布

改讲义 → `.deploy/request.json` 的 `nonce` +1 → push → 等 GitHub Actions `release` 全绿（含线上冒烟测试）。详见 README。线上地址：https://intro-neusci.hypergrid.workers.dev/

## 技术要点

- 讲义是无 doctype 的 HTML 片段（构建时由 `scripts/build.mjs` 包装），全部 CSS/JS 内联，唯一外部依赖是 Google Fonts（有系统字体回退）。
- 双主题（浅/深）走 CSS token 三段式（`:root` / `@media dark :root:not([data-theme="light"])` / `:root[data-theme="dark"]`）；仪器面板（示波器类）固定深色。
- 课堂现场投票：预测组件在同源 `/health` 应答时自动启用（`/api/vote`、`/api/votes`，Durable Object 计票）；其余环境静默退回纯本地。新讲义的预测题用唯一 `data-predict` id，并把 id 加进 `site/admin.html` 的 QIDS。
- 模拟参数必须先用 Node 数值验证（阈值、时间窗、滑杆范围要与真实模拟一致），交互判定不许拍脑袋。
